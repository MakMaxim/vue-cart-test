export default {
   PRODUCTS(state) {
      return state.products;
   },
   CART(state) {
      return state.cart;
   },
   DELETED(state) {
      return state.deleted;
   },
   WIDTH(state) {
      return state.width;
   },
   HEIGHT(state) {
      return state.height;
   },
};
